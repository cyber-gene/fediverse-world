{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt/fonts": "^0.14.0",
    "nuxt": "^4.4.2",
    "vue": "^3.5.32"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
